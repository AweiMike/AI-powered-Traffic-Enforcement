# 精準執法儀表板系統 - EXE 啟動器使用說明

## 🎯 方式一：使用圖形化啟動器（推薦）

### 打包成 EXE

1. **執行打包腳本**：
   - 雙擊 `打包成EXE.bat`
   - 等待打包完成（約需 1-2 分鐘）

2. **找到執行檔**：
   - 打包完成後，執行檔位於：`dist/精準執法儀表板系統.exe`

3. **複製到根目錄**（可選）：
   ```
   複製：dist/精準執法儀表板系統.exe
   到：  D:\Programming\精準執法儀表板系統\
   ```

### 使用 EXE 啟動器

雙擊 `精準執法儀表板系統.exe`，會開啟圖形化介面：

#### 主要功能

1. **服務狀態監控**
   - 🔧 後端 API 狀態
   - 🌐 前端介面狀態
   - 🗄️ 資料庫連接狀態
   - 自動每 3 秒更新狀態

2. **快速啟動**
   - 🌿 **啟動完整系統** - 一鍵啟動前後端，自動開啟瀏覽器
   - 🛑 **停止所有服務** - 安全停止所有運行中的服務
   - ▶️ **僅啟動後端** - 只啟動 FastAPI 後端
   - ▶️ **僅啟動前端** - 只啟動 React 前端

3. **系統工具**
   - 🗄️ **初始化資料庫** - 創建資料表和初始資料
   - 📊 **匯入資料** - 從 Excel 匯入資料（會彈出檔案選擇對話框）
   - 📚 **開啟 API 文件** - 在瀏覽器開啟 Swagger 文件

4. **快速連結**
   - 點擊任一連結可直接在瀏覽器開啟
   - 🌐 前端儀表板：http://localhost:5173
   - 📡 後端 API：http://localhost:8000
   - 📚 API 文件：http://localhost:8000/docs

### 優點

✅ **視覺化操作** - 不需要記指令，點擊按鈕即可
✅ **狀態監控** - 即時顯示各服務運行狀態
✅ **中文介面** - 所有文字都是中文
✅ **動森風格** - 使用動物森友會配色
✅ **一鍵啟動** - 點一下就能啟動整個系統

---

## 🎯 方式二：使用批次檔（傳統方式）

### 主啟動器

雙擊 `啟動系統.bat`，會顯示文字選單：

```
[1] 啟動完整系統（前端 + 後端）
[2] 僅啟動後端
[3] 僅啟動前端
[4] 初始化資料庫
[5] 匯入資料
[6] 開啟 API 文件
[7] 開啟前端網頁
[0] 退出
```

輸入數字選擇功能。

### 分別啟動

- **啟動後端**：雙擊 `啟動後端.bat`
- **啟動前端**：雙擊 `啟動前端.bat`
- **停止系統**：雙擊 `停止系統.bat`

---

## 📊 比較：EXE vs 批次檔

| 功能 | EXE 啟動器 | 批次檔 |
|------|-----------|--------|
| 圖形介面 | ✅ 是 | ❌ 否（命令列） |
| 狀態監控 | ✅ 即時顯示 | ❌ 無 |
| 操作方式 | 點擊按鈕 | 輸入數字 |
| 自動開啟瀏覽器 | ✅ 是 | ✅ 是 |
| 檔案選擇對話框 | ✅ 是 | ❌ 否（手動輸入） |
| 需要打包 | ✅ 需要 | ❌ 不需要 |
| 檔案大小 | 較大（~20MB） | 小（<10KB） |

---

## 🔧 首次使用步驟

### 使用 EXE 啟動器

1. **打包 EXE**（首次）
   - 雙擊 `打包成EXE.bat`
   - 等待打包完成

2. **初始化資料庫**
   - 雙擊 `精準執法儀表板系統.exe`
   - 點擊「🗄️ 初始化資料庫」

3. **啟動系統**
   - 點擊「🌿 啟動完整系統」
   - 等待瀏覽器自動開啟

### 使用批次檔

1. **初始化資料庫**
   - 雙擊 `啟動系統.bat`
   - 選擇 `[4]`

2. **啟動系統**
   - 雙擊 `啟動系統.bat`
   - 選擇 `[1]`

---

## 🐛 常見問題

### Q: 打包時出現錯誤

**A**: 確認已安裝所需套件：
```bash
pip install -r requirements_launcher.txt
```

### Q: EXE 執行時找不到檔案

**A**: 確保 exe 與以下目錄在同一層級：
```
精準執法儀表板系統/
├── 精準執法儀表板系統.exe  ← 這裡
├── backend/
└── animal-crossing-dashboard/
```

### Q: 啟動器顯示「虛擬環境不存在」

**A**: 首次使用請先執行：
- 雙擊 `啟動後端.bat`（會自動創建虛擬環境）
- 或手動執行：
  ```bash
  cd backend
  python -m venv venv
  venv\Scripts\activate
  pip install -r requirements.txt
  ```

### Q: 前端顯示「依賴未安裝」

**A**: 首次使用請先執行：
- 雙擊 `啟動前端.bat`（會自動安裝依賴）
- 或手動執行：
  ```bash
  cd animal-crossing-dashboard
  npm install
  ```

### Q: 想修改 EXE 圖標

**A**: 編輯 `build_exe.py`：
```python
"--icon=your_icon.ico",  # 替換這行
```

---

## 💡 推薦使用方式

### 日常使用（推薦 EXE）

✅ 使用 `精準執法儀表板系統.exe`
- 視覺化操作更直觀
- 狀態監控更方便
- 適合非技術人員

### 開發調試（推薦批次檔）

✅ 使用批次檔或直接啟動
- 更容易看到詳細錯誤訊息
- 更快速重啟服務
- 適合開發人員

---

## 📝 注意事項

1. **首次啟動較慢**
   - 需要載入環境和依賴
   - 約需 5-10 秒

2. **Port 衝突**
   - 前端：5173
   - 後端：8000
   - 資料庫：5432
   - 如有衝突請修改配置

3. **關閉啟動器不會停止服務**
   - 點擊「🛑 停止所有服務」才會停止
   - 或使用 `停止系統.bat`

4. **EXE 檔案大小**
   - 約 15-25MB（包含所有依賴）
   - 正常現象，不影響使用

---

**版本**: v1.0.0
**更新日期**: 2026-01-14

🌿 讓執法更精準，操作更簡單！
