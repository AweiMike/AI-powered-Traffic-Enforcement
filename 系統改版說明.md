# 系統改版說明 - SQLite 版本

## 📋 改版摘要

因公司電腦沒有系統權限安裝 PostgreSQL，系統已改為使用 **SQLite 資料庫**。

### 主要變更

| 項目 | 原版本 | 新版本 |
|------|--------|--------|
| 資料庫 | PostgreSQL + PostGIS | SQLite 3 |
| 安裝需求 | 需要系統權限安裝 PostgreSQL | 無需安裝，直接使用 |
| 資料儲存 | 資料庫伺服器 | 單一 `.db` 檔案 |
| 地理空間 | PostGIS 擴充套件 | 經緯度欄位 |
| 網路分享 | 需設定資料庫遠端連線 | 透過 API 分享 |

## ✅ 已完成的工作

### 1. 後端調整

#### 資料庫配置 ([config.py](backend/app/config.py))
```python
# 從 PostgreSQL 改為 SQLite
DATABASE_URL: str = "sqlite:///./data/traffic_enforcement.db"
```

#### 資料庫連線 ([database.py](backend/app/database.py))
- 移除 `geoalchemy2` 依賴
- 添加 SQLite 專用配置 (`check_same_thread=False`)

#### 資料模型調整
- [core.py](backend/app/models/core.py): 移除 `Geometry` 欄位，改用 `latitude` 和 `longitude`
- [dimension.py](backend/app/models/dimension.py): 同樣移除 PostGIS 依賴

#### 初始化腳本 ([init_sqlite.py](backend/scripts/init_sqlite.py))
- 創建 SQLite 專用初始化腳本
- 自動創建 `data` 目錄
- 初始化基礎資料（班別、主題、違規條款）

#### API 健康檢查 ([main.py](backend/app/main.py))
```python
# 返回資料庫類型資訊
{
  "status": "ok",
  "database": "sqlite",  # 或 "postgresql"
  "mode": "full"         # 區別於 simple 模擬模式
}
```

### 2. 前端調整

#### 已完成（前次更新）
- ✅ 移除「交通流量」功能
- ✅ 更新為「事故與違規分析」主題
- ✅ 所有組件實際連接 API
- ✅ 添加後端連線狀態指示器
- ✅ 完整錯誤處理

前端無需額外修改，API 介面保持一致。

### 3. 啟動腳本

#### [初始化資料庫.bat](初始化資料庫.bat)
快速初始化 SQLite 資料庫的捷徑。

#### [啟動系統.bat](啟動系統.bat)
已更新選項 [4] 使用 SQLite 初始化腳本。

### 4. 文件

#### [使用說明-SQLite版本.md](使用說明-SQLite版本.md)
- 快速開始指南
- 資料庫位置與備份
- 讓其他電腦連接的方法
- 常見問題解答

#### [網路分享設定指南.md](網路分享設定指南.md)
- 查詢主機 IP
- 設定 Windows 防火牆
- 修改 CORS 設定
- 測試連線
- 問題排解

## 🚀 使用方式

### 首次使用

1. **初始化資料庫**
   ```batch
   雙擊執行：初始化資料庫.bat
   ```

2. **啟動系統**
   ```batch
   雙擊執行：啟動系統.bat
   選擇 [1] 啟動完整系統
   ```

3. **訪問系統**
   - 前端：http://localhost:5173
   - 後端：http://localhost:8000
   - 文件：http://localhost:8000/docs

### 讓其他電腦連接

請參考 [網路分享設定指南.md](網路分享設定指南.md)

簡要步驟：
1. 查詢本機 IP（例如：192.168.1.100）
2. 設定防火牆允許 Port 8000, 5173
3. 修改 CORS 設定
4. 其他電腦訪問：http://192.168.1.100:5173

## 📁 檔案結構

```
精準執法儀表板系統/
├── backend/
│   ├── app/
│   │   ├── config.py          ← 已修改：SQLite 設定
│   │   ├── database.py        ← 已修改：SQLite 連線
│   │   ├── main.py            ← 已修改：健康檢查
│   │   └── models/
│   │       ├── core.py        ← 已修改：移除 PostGIS
│   │       ├── dimension.py   ← 已修改：移除 PostGIS
│   │       └── aggregate.py
│   ├── scripts/
│   │   └── init_sqlite.py     ← 新增：SQLite 初始化
│   └── data/
│       └── traffic_enforcement.db  ← 資料庫檔案（執行後產生）
│
├── animal-crossing-dashboard/
│   └── src/
│       ├── App.tsx            ← 已修改：移除車流量
│       ├── api/client.ts      ← 已完成
│       ├── hooks/useAPI.ts    ← 已完成
│       └── components/        ← 已完成
│
├── 初始化資料庫.bat           ← 新增
├── 啟動系統.bat               ← 已修改
├── 啟動系統-簡化版.bat        ← 已存在
├── 使用說明-SQLite版本.md     ← 新增
├── 網路分享設定指南.md        ← 新增
└── 系統改版說明.md            ← 本檔案
```

## 🔧 技術細節

### SQLite vs PostgreSQL

| 特性 | SQLite | PostgreSQL |
|------|--------|-----------|
| 安裝 | 無需安裝 | 需要安裝 |
| 權限 | 無需系統權限 | 需要系統管理員權限 |
| 資料量 | 適合中小型（<50萬筆） | 適合大型 |
| 並發 | 讀取無限制，寫入有限制 | 完全支援高並發 |
| 備份 | 複製檔案 | 需要 pg_dump |
| 地理空間 | 需手動實作 | PostGIS 原生支援 |

### 資料模型變更

#### 舊版（PostgreSQL + PostGIS）
```python
from geoalchemy2 import Geometry

class Crash(Base):
    geom = Column(Geometry('POINT', srid=4326))
```

#### 新版（SQLite）
```python
class Crash(Base):
    latitude = Column(Float)
    longitude = Column(Float)
```

### 為什麼移除 PostGIS？

1. **依賴複雜**：PostGIS 需要 PostgreSQL，SQLite 沒有原生地理空間支援
2. **使用有限**：本系統主要用於統計分析，不需要複雜的地理空間查詢
3. **簡化替代**：使用經緯度欄位足以滿足需求（點位聚合、距離計算）

## 📊 效能評估

### 預期資料量
- 交通事故：每年約 500-1000 筆
- 舉發案件：每年約 5000-10000 筆
- 保留 3 年資料：約 3 萬筆

### SQLite 效能
- 查詢速度：毫秒級
- 插入速度：每秒數千筆
- 資料庫大小：約 10-50 MB

**結論**：SQLite 完全足夠！

### 何時需要升級到 PostgreSQL？

- 資料量超過 50 萬筆
- 需要複雜的地理空間查詢
- 需要高並發寫入
- 需要進階功能（分區表、物化視圖等）

## 🔄 回退到 PostgreSQL（如未來需要）

如果未來有系統權限，可以輕鬆回退：

1. **安裝 PostgreSQL**
2. **修改 config.py**
   ```python
   DATABASE_URL: str = "postgresql://postgres:postgres@localhost:5432/traffic_enforcement"
   ```
3. **恢復 PostGIS 模型**（保留了經緯度欄位，兩者可共存）
4. **執行 init_database.py**（PostgreSQL 版）
5. **匯入資料**

## ⚠️ 注意事項

### 1. 資料備份
SQLite 資料庫是單一檔案，**務必定期備份**：
```batch
copy backend\data\traffic_enforcement.db backup\traffic_enforcement_20250114.db
```

### 2. 主機保持運行
這台電腦就是資料庫主機，關機後其他電腦無法連線。

### 3. 防火牆設定
如果其他電腦要連線，必須設定防火牆允許：
- Port 8000（後端 API）
- Port 5173（前端介面）

### 4. 效能監控
如果系統變慢，可能需要：
- 清理舊資料
- 執行 VACUUM 清理
- 或考慮升級到 PostgreSQL

## 📞 問題回報

如遇問題，請檢查：
1. 後端視窗的錯誤訊息
2. 前端 Console (F12)
3. 資料庫檔案是否存在：`backend/data/traffic_enforcement.db`

---

**改版日期**：2025-01-14
**改版原因**：無系統權限安裝 PostgreSQL
**解決方案**：改用 SQLite 資料庫
**版本**：1.0.0-SQLite
