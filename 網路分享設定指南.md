# 網路分享設定指南

## 目標

讓區域網路內的其他電腦能夠透過瀏覽器訪問本系統。

```
主機電腦（這台）                     其他電腦
┌─────────────────┐                ┌─────────────────┐
│ 後端 API        │                │                 │
│ Port: 8000      │◄───────────────┤ 瀏覽器          │
│                 │                │                 │
│ 前端介面        │                │ 訪問:           │
│ Port: 5173      │◄───────────────┤ http://主機IP   │
│                 │                │       :5173     │
│ SQLite DB       │                │                 │
└─────────────────┘                └─────────────────┘
```

## 步驟 1：查詢主機 IP 位址

### Windows 10/11

1. 按 `Win + R` 開啟執行視窗
2. 輸入 `cmd` 按 Enter
3. 在命令提示字元輸入：
   ```batch
   ipconfig
   ```
4. 找到「乙太網路卡」或「無線區域網路」的 **IPv4 位址**

範例輸出：
```
無線區域網路介面卡 Wi-Fi:
   IPv4 位址 . . . . . . . . . . . . : 192.168.1.100
                                      ^^^^^^^^^^^^^ 這個就是！
```

**記下這個 IP 位址**，例如：`192.168.1.100`

## 步驟 2：設定 Windows 防火牆

### 方式 A：使用命令列（快速，推薦）

**以管理員身分執行命令提示字元**：
1. 按 `Win + X`
2. 選擇「Windows PowerShell (系統管理員)」或「命令提示字元 (系統管理員)」

執行以下命令：

```batch
# 允許 Port 8000 (後端 API)
netsh advfirewall firewall add rule name="精準執法系統-後端API" dir=in action=allow protocol=TCP localport=8000

# 允許 Port 5173 (前端介面)
netsh advfirewall firewall add rule name="精準執法系統-前端介面" dir=in action=allow protocol=TCP localport=5173
```

### 方式 B：使用圖形界面

1. **開啟 Windows 防火牆設定**
   - 控制台 → 系統及安全性 → Windows Defender 防火牆
   - 點擊左側「進階設定」

2. **新增輸入規則（Port 8000）**
   - 點擊「輸入規則」→「新增規則」
   - 規則類型：選擇「連接埠」→ 下一步
   - 協定：TCP，特定本機連接埠：`8000`
   - 動作：允許連線
   - 設定檔：全部勾選
   - 名稱：`精準執法系統-後端API`

3. **新增輸入規則（Port 5173）**
   - 重複上述步驟
   - 連接埠改為：`5173`
   - 名稱：`精準執法系統-前端介面`

### 驗證防火牆規則

```batch
# 查看已新增的規則
netsh advfirewall firewall show rule name="精準執法系統-後端API"
netsh advfirewall firewall show rule name="精準執法系統-前端介面"
```

## 步驟 3：修改 CORS 設定

編輯 `backend/app/config.py` 檔案：

```python
# CORS
ALLOWED_ORIGINS: List[str] = [
    "http://localhost:5173",
    "http://127.0.0.1:5173",
    "http://192.168.1.100:5173",  # ← 添加主機 IP（改成你的 IP）
    "http://192.168.1.*",          # ← 允許整個區網 192.168.1.x（可選）
]
```

**修改後記得重啟後端服務！**

## 步驟 4：啟動系統

執行 `啟動系統.bat` 啟動完整系統。

確認兩個視窗都正常運行：
- 後端 API 視窗
- 前端介面視窗

## 步驟 5：從其他電腦測試連線

### 測試 1：Ping 測試

在其他電腦開啟命令提示字元：
```batch
ping 192.168.1.100
```

如果能 ping 通，表示網路連線正常。

### 測試 2：訪問後端 API

在其他電腦的瀏覽器輸入：
```
http://192.168.1.100:8000/health
```

應該會看到 JSON 回應：
```json
{
  "status": "ok",
  "service": "精準執法儀表板系統",
  "database": "sqlite",
  "mode": "full"
}
```

### 測試 3：訪問前端介面（完整系統）

在其他電腦的瀏覽器輸入：
```
http://192.168.1.100:5173
```

應該會看到動森風格的儀表板介面。

## 步驟 6：分享給其他使用者

告訴其他使用者訪問網址：
```
http://192.168.1.100:5173
```

**建議將網址加入瀏覽器的「我的最愛」**

## 🔧 進階設定

### 設定固定 IP（推薦）

避免主機 IP 變動，導致其他電腦連不上：

1. **開啟網路設定**
   - 控制台 → 網路和網際網路 → 網路連線
   - 右鍵點擊使用中的網路介面卡 → 內容

2. **設定 IPv4**
   - 選擇「Internet Protocol Version 4 (TCP/IPv4)」→ 內容
   - 選擇「使用下列的 IP 位址」
   - IP 位址：`192.168.1.100` (原本查到的 IP)
   - 子網路遮罩：`255.255.255.0`
   - 預設閘道：`192.168.1.1` (通常是路由器 IP)
   - 慣用 DNS：`8.8.8.8` (Google DNS)

### 設定內部網域名稱（選用）

如果公司有內部 DNS 伺服器，可以設定易記的網域名稱：

```
traffic-dashboard.internal → 192.168.1.100
```

這樣其他人就能用：
```
http://traffic-dashboard.internal:5173
```

### 使用反向代理（進階）

如果希望只開放單一 Port（如 80），可以使用 Nginx 反向代理：

```nginx
server {
    listen 80;
    server_name 192.168.1.100;

    location / {
        proxy_pass http://localhost:5173;
    }

    location /api {
        proxy_pass http://localhost:8000;
    }
}
```

## 🛠️ 常見問題排解

### Q1: 其他電腦顯示「無法連線」

**檢查清單**：
1. ✅ 主機系統是否正在運行？
2. ✅ 防火牆規則是否已新增？
3. ✅ IP 位址是否正確？
4. ✅ 兩台電腦是否在同一區網？

**測試方法**：
```batch
# 在其他電腦執行
ping 192.168.1.100
telnet 192.168.1.100 8000
telnet 192.168.1.100 5173
```

### Q2: 前端能開啟，但 API 無法連線

**原因**：CORS 設定問題

**解決**：
1. 檢查 `backend/app/config.py` 的 `ALLOWED_ORIGINS`
2. 確認有添加主機 IP
3. **重啟後端服務**（這很重要！）

### Q3: 有時候能連，有時候不能連

**原因**：主機 IP 變動了

**解決**：
1. 設定固定 IP（見「進階設定」）
2. 或修改 CORS 允許整個區網：`http://192.168.1.*`

### Q4: 公司網路無法連線

**可能原因**：
1. 公司防火牆阻擋
2. 不在同一 VLAN
3. 需要網管協助開通

**建議**：
- 聯繫 IT 部門說明需求
- 提供需要開通的 Port：8000, 5173

### Q5: 外網可以訪問嗎？

**不建議！** 此系統設計為內網使用。

如果真的需要外網訪問：
1. 需要設定路由器 Port Forwarding
2. 必須加上身分驗證機制
3. 建議使用 HTTPS
4. 考慮資料安全風險

## 📊 連線狀態監控

### 查看誰正在使用系統

在後端視窗可以看到訪問日誌：
```
INFO:     192.168.1.50:54321 - "GET /api/v1/stats/overview HTTP/1.1" 200 OK
INFO:     192.168.1.51:54322 - "GET /api/v1/recommendations/top5 HTTP/1.1" 200 OK
```

## 🔒 安全建議

### 1. 限制訪問來源

在 CORS 設定中只允許特定 IP：
```python
ALLOWED_ORIGINS: List[str] = [
    "http://192.168.1.50:5173",  # 只允許這台電腦
    "http://192.168.1.51:5173",  # 和這台
]
```

### 2. 設定唯讀帳號（進階）

如果需要更嚴格的權限控制，可以：
1. 新增 API 金鑰驗證
2. 設定不同權限等級
3. 記錄操作日誌

### 3. 定期備份

確保資料安全：
```batch
# 每日自動備份腳本
copy backend\data\traffic_enforcement.db "\\備份伺服器\backups\%date:~0,10%_traffic_db.db"
```

## 📝 給使用者的說明範本

您可以將以下說明發給其他使用者：

---

**精準執法儀表板系統 - 使用說明**

請在瀏覽器輸入以下網址：
```
http://192.168.1.100:5173
```

**建議瀏覽器**：
- Google Chrome
- Microsoft Edge
- Firefox

**注意事項**：
1. 此為內網系統，需連接公司網路
2. 主機電腦關機時無法使用
3. 有問題請聯繫 IT 或系統管理員

---

## 🎯 快速設定檢查表

完成以下項目即可讓其他電腦連線：

- [ ] 查詢主機 IP 位址
- [ ] 設定防火牆規則（Port 8000, 5173）
- [ ] 修改 CORS 設定
- [ ] 重啟後端服務
- [ ] 測試從其他電腦連線
- [ ] 將網址分享給使用者

---

**最後更新**：2025-01-14
