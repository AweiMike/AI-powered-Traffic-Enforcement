# 精準執法儀表板系統 - 完整部署指南

> 📅 最後更新：2026-01-14
> 🔒 個資保護等級：高（完全去識別化）

---

## 📋 目錄

1. [系統概述](#系統概述)
2. [環境需求](#環境需求)
3. [後端部署](#後端部署)
4. [前端部署](#前端部署)
5. [資料匯入](#資料匯入)
6. [驗證測試](#驗證測試)
7. [常見問題](#常見問題)

---

## 系統概述

### 🎯 系統定位

**統計分析 + 精準執法建議工具（無個資）**

- ✅ 統計去年同期比較
- ✅ 分析違規態勢率
- ✅ 精準打擊酒駕
- ✅ 高齡者事故防治
- ✅ Top 5 點位推薦
- ❌ 不包含個案查詢

### 🏗️ 系統架構

```
┌─────────────────────────────┐
│  Frontend (React + Vite)    │
│  Port: 3000                 │
│  動森風格 UI                 │
└────────────┬────────────────┘
             │ HTTP/REST API
┌────────────┴────────────────┐
│  Backend (FastAPI)          │
│  Port: 8000                 │
│  API 端點 + 指標計算         │
└────────────┬────────────────┘
             │ SQL
┌────────────┴────────────────┐
│  Database (PostgreSQL)      │
│  Port: 5432                 │
│  PostGIS 擴展               │
│  完全去識別化資料            │
└─────────────────────────────┘
```

---

## 環境需求

### 必要軟體

| 軟體 | 版本需求 | 用途 |
|------|---------|------|
| **Python** | 3.10+ | 後端執行環境 |
| **Node.js** | 16+ | 前端構建工具 |
| **PostgreSQL** | 14+ | 資料庫 |
| **PostGIS** | 3.0+ | 地理空間擴展 |
| **Git** | 最新版 | 版本控制 |

### 硬體建議

- **CPU**: 4核心以上
- **RAM**: 8GB 以上
- **硬碟**: 20GB 可用空間
- **網路**: 100Mbps 以上

---

## 後端部署

### 步驟 1：環境設定

```bash
# 1. 進入後端目錄
cd backend

# 2. 創建虛擬環境
python -m venv venv

# 3. 啟動虛擬環境
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# 4. 安裝依賴
pip install -r requirements.txt
```

### 步驟 2：PostgreSQL 設定

#### 安裝 PostgreSQL

**Windows:**
1. 下載 PostgreSQL 安裝程式：https://www.postgresql.org/download/windows/
2. 執行安裝程式，記住設定的密碼
3. 預設 Port: 5432

**macOS (使用 Homebrew):**
```bash
brew install postgresql@14
brew services start postgresql@14
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib postgis
sudo systemctl start postgresql
```

#### 創建資料庫

```bash
# 連線到 PostgreSQL
psql -U postgres

# 創建資料庫
CREATE DATABASE traffic_enforcement;

# 創建使用者（選用）
CREATE USER enforcement_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE traffic_enforcement TO enforcement_user;

# 退出
\q
```

#### 啟用 PostGIS 擴展

```bash
# 連線到資料庫
psql -U postgres -d traffic_enforcement

# 啟用 PostGIS
CREATE EXTENSION postgis;

# 驗證
SELECT PostGIS_version();

# 退出
\q
```

### 步驟 3：環境變數設定

```bash
# 複製環境變數範本
cp .env.example .env

# 編輯 .env
```

`.env` 內容範例：

```env
# 資料庫設定
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/traffic_enforcement
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=your_password
DATABASE_NAME=traffic_enforcement

# API 設定
API_V1_PREFIX=/api/v1
DEBUG=True

# CORS 設定（允許前端存取）
ALLOWED_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]

# 安全設定
SECRET_KEY=請更換為隨機字串
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

### 步驟 4：初始化資料庫

```bash
# 執行初始化腳本
python backend/scripts/init_database.py

# 預期輸出：
# ✅ PostgreSQL 連線成功
# ✅ PostGIS 擴展已啟用
# ✅ 所有資料表創建完成（12 個表）
# ✅ 班別資料初始化完成（12 筆）
# ✅ 違規條款資料初始化完成（7 筆示例）
```

### 步驟 5：啟動後端伺服器

```bash
# 方法 1：使用 main.py（推薦開發用）
python backend/app/main.py

# 方法 2：使用 uvicorn
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 預期輸出：
# 🌿 精準執法儀表板系統 啟動中...
# 📍 API 端點：http://localhost:8000/api/v1
# 📚 API 文件：http://localhost:8000/docs
```

### 步驟 6：驗證後端

開啟瀏覽器訪問：

- **API 文件**: http://localhost:8000/docs
- **系統資訊**: http://localhost:8000/
- **健康檢查**: http://localhost:8000/health

---

## 前端部署

### 步驟 1：安裝依賴

```bash
# 進入前端目錄
cd animal-crossing-dashboard

# 安裝 npm 套件
npm install
```

### 步驟 2：環境變數設定

```bash
# 複製環境變數範本
cp .env.example .env

# 編輯 .env
```

`.env` 內容：

```env
# 後端 API 地址
VITE_API_BASE_URL=http://localhost:8000

# 除錯模式（選用）
VITE_DEBUG=false
```

### 步驟 3：啟動開發伺服器

```bash
# 啟動 Vite 開發伺服器
npm run dev

# 預期輸出：
# VITE v4.x.x  ready in xxx ms
# ➜  Local:   http://localhost:3000/
# ➜  Network: use --host to expose
```

### 步驟 4：構建生產版本

```bash
# 構建前端
npm run build

# 輸出目錄：dist/
```

### 步驟 5：預覽生產版本

```bash
# 預覽構建結果
npm run preview
```

### 步驟 6：驗證前端

開啟瀏覽器訪問：http://localhost:3000

應該看到動森風格的儀表板介面。

---

## 資料匯入

### 準備 Excel 檔案

確保 Excel 檔案包含以下欄位：

#### 交通事故檔案
- 發生時間（日期時間）
- 發生地點（完整地址）
- 類別名稱（A1/A2/A3）
- 年齡（選用）
- 性別（選用）

#### 舉發案件檔案
- 違規時間（日期時間）
- 違規地點（完整地址）
- 違規條款代碼
- 違規條款名稱
- 年齡（選用）
- 性別（選用）

### 執行匯入腳本

```bash
# 確保在後端目錄且虛擬環境已啟動
cd backend
source venv/bin/activate  # Windows: venv\Scripts\activate

# 執行匯入
python backend/scripts/import_data.py \
  --crash-file "data/交通事故案件清冊_已處理.xlsx" \
  --ticket-file "data/舉發案件綜合查詢_已處理.xlsx" \
  --cluster-radius 100

# 參數說明：
# --crash-file: 交通事故 Excel 檔案路徑
# --ticket-file: 舉發案件 Excel 檔案路徑
# --cluster-radius: 點位聚合半徑（公尺），預設 100
```

### 匯入過程說明

1. **地址去識別化**: 自動移除門牌號、巷弄號
2. **年齡分組**: 將精確年齡轉換為年齡組（<18/18-24/25-44/45-64/65+）
3. **主題識別**: 自動識別酒駕、闖紅燈、危險駕駛主題
4. **點位聚合**: 將相近案件聚合成執法點位
5. **班別計算**: 根據時間計算12班制班別

### 驗證匯入結果

```bash
# 連線到資料庫
psql -U postgres -d traffic_enforcement

# 檢查資料筆數
SELECT COUNT(*) FROM core_crash;
SELECT COUNT(*) FROM core_ticket;
SELECT COUNT(*) FROM dim_site;

# 檢查主題分布
SELECT
  SUM(CASE WHEN topic_dui THEN 1 ELSE 0 END) AS dui_count,
  SUM(CASE WHEN topic_red_light THEN 1 ELSE 0 END) AS red_light_count,
  SUM(CASE WHEN topic_dangerous THEN 1 ELSE 0 END) AS dangerous_count
FROM core_ticket;

# 驗證無個資
SELECT * FROM core_crash LIMIT 5;
SELECT * FROM core_ticket LIMIT 5;
```

---

## 驗證測試

### 1. 後端 API 測試

#### 使用 Swagger UI（推薦）

訪問 http://localhost:8000/docs

測試以下端點：

1. **系統資訊**: `GET /`
2. **總覽統計**: `GET /api/v1/stats/overview?days=30`
3. **Top 5 推薦**: `GET /api/v1/recommendations/top5?topic_code=DUI&shift_id=11&days=30`
4. **月度統計**: `GET /api/v1/stats/monthly?year=2025&month=1`

#### 使用 curl

```bash
# 系統資訊
curl http://localhost:8000/

# 總覽統計
curl "http://localhost:8000/api/v1/stats/overview?days=30"

# Top 5 推薦
curl "http://localhost:8000/api/v1/recommendations/top5?topic_code=DUI&shift_id=11&days=30"

# 班前勤務建議卡
curl "http://localhost:8000/api/v1/recommendations/briefing-card?topic_code=DUI&shift_id=11"
```

### 2. 前端功能測試

#### 總覽頁面
- ✅ 統計卡片顯示正常
- ✅ 月度比較圖表顯示
- ✅ 無錯誤訊息

#### Top 5 推薦頁面
- ✅ 主題選擇器可用
- ✅ 班別選擇器可用
- ✅ Top 5 卡片顯示
- ✅ VPI/CRI/Score 正確計算

#### 班前勤務卡頁面
- ✅ 勤務建議卡生成
- ✅ Top 5 點位顯示
- ✅ 注意事項顯示

### 3. 個資保護驗證

檢查所有 API 回應和前端顯示：

- ❌ 無姓名、身分證、車號
- ❌ 無完整地址（無門牌號）
- ❌ 無精確年齡（僅年齡組）
- ✅ 每個回應都有 `note` 欄位說明無個資

---

## 常見問題

### Q1: PostgreSQL 連線失敗

**症狀**: `connection refused` 或 `could not connect to server`

**解決方案**:
```bash
# 檢查 PostgreSQL 是否運行
# Windows:
services.msc  # 查找 postgresql 服務

# macOS/Linux:
sudo systemctl status postgresql
# 或
ps aux | grep postgres

# 重啟 PostgreSQL
# Windows: 在服務管理中重啟
# macOS:
brew services restart postgresql@14
# Linux:
sudo systemctl restart postgresql
```

### Q2: PostGIS 擴展安裝失敗

**症狀**: `ERROR:  could not open extension control file`

**解決方案**:
```bash
# Windows: 確保安裝時勾選了 PostGIS 擴展
# 重新執行 PostgreSQL 安裝程式

# macOS:
brew install postgis

# Linux:
sudo apt install postgis postgresql-14-postgis-3
```

### Q3: 前端無法連接後端 API

**症狀**: 前端顯示 `Network Error` 或 `CORS Error`

**解決方案**:

1. 檢查後端是否運行：訪問 http://localhost:8000/docs
2. 檢查 CORS 設定：後端 `config.py` 中的 `ALLOWED_ORIGINS`
3. 檢查前端 `.env` 中的 `VITE_API_BASE_URL`

```python
# backend/app/config.py
ALLOWED_ORIGINS: List[str] = [
    "http://localhost:3000",
    "http://127.0.0.1:3000",
]
```

### Q4: 資料匯入時出現編碼錯誤

**症狀**: `UnicodeDecodeError` 或亂碼

**解決方案**:

1. 確保 Excel 檔案是 UTF-8 編碼
2. 使用 Excel 另存為時選擇「CSV UTF-8」
3. 或在腳本中指定編碼：

```python
df = pd.read_excel(file_path, encoding='utf-8')
```

### Q5: 前端構建失敗

**症狀**: `npm run build` 失敗

**解決方案**:

```bash
# 清除 node_modules 和 lock 文件
rm -rf node_modules package-lock.json

# 重新安裝
npm install

# 清除快取
npm cache clean --force

# 重新構建
npm run build
```

### Q6: Top 5 推薦無結果

**症狀**: API 返回空陣列

**可能原因**:
1. 資料庫無資料或資料太少
2. 篩選條件太嚴格（特定班別無資料）
3. 點位聚合未完成

**解決方案**:

```bash
# 檢查資料量
psql -U postgres -d traffic_enforcement
SELECT COUNT(*) FROM core_ticket WHERE topic_dui = TRUE AND shift_id = '11';

# 如果為 0，嘗試：
# 1. 調整班別（選擇全時段）
# 2. 重新匯入資料
# 3. 檢查主題識別邏輯
```

### Q7: 系統效能緩慢

**症狀**: API 回應時間 > 5 秒

**優化建議**:

1. **建立資料庫索引**（已在模型中定義）
2. **使用聚合統計表**：定期執行 `calculate_site_metrics()`
3. **增加資料庫連線池**：調整 `config.py` 中的 `pool_size`
4. **快取查詢結果**：使用 Redis（選用）

---

## 生產環境部署建議

### 安全設定

1. **更換 SECRET_KEY**: 使用隨機字串
2. **關閉 DEBUG 模式**: 設定 `DEBUG=False`
3. **使用 HTTPS**: 配置 SSL 憑證
4. **限制 CORS**: 僅允許特定域名

### 效能優化

1. **使用 Gunicorn 或 uWSGI** 作為 WSGI 伺服器
2. **使用 Nginx** 作為反向代理
3. **配置資料庫連線池**
4. **啟用 Gzip 壓縮**

### 監控與維護

1. **設定日誌記錄**: 記錄 API 請求和錯誤
2. **監控系統資源**: CPU、記憶體、硬碟
3. **定期備份資料庫**
4. **定期更新聚合統計表**

---

## 系統維護

### 每日維護

- 檢查系統運行狀態
- 檢查錯誤日誌

### 每週維護

- 更新聚合統計表
- 檢查資料庫效能
- 備份資料庫

### 每月維護

- 清理舊日誌
- 檢查硬碟空間
- 更新依賴套件

---

## 聯絡資訊

如有問題請聯繫：系統管理員

---

**版本**: v1.0.0
**更新日期**: 2026-01-14
**個資保護等級**: 高（完全去識別化）

🔒 **本系統設計確保無個資外洩風險**
