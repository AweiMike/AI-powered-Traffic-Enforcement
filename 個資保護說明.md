# 個資保護說明文件

> 📅 更新日期：2026-01-13
> 🔒 系統定位：統計分析 + 精準執法建議工具（不儲存個資）

---

## 🎯 系統定位與目標

### 核心功能
- ✅ 統計去年同期比較
- ✅ 分析哪些違規造勢率偏高
- ✅ 精準打擊酒後駕車及肇事
- ✅ 高齡者交通事故防治
- ✅ 輔助分局轄內各單位精準執法

### 不包含功能
- ❌ 個案查詢（已有內建系統）
- ❌ 個人資料管理
- ❌ 案件追蹤

---

## 🔒 個資保護措施

### 一、完全移除的個資

#### 交通事故案件
- ❌ 案件編號（使用系統自動ID取代）
- ❌ 當事人姓名
- ❌ 當事人身分證字號
- ❌ 當事人聯絡方式（電話、地址）
- ❌ 車號（完整移除）
- ❌ 駕照號碼
- ❌ 承辦人姓名
- ❌ 承辦人聯絡方式

#### 舉發案件
- ❌ 舉發單號（使用系統自動ID取代）
- ❌ 車號（完整移除）
- ❌ 駕駛人姓名
- ❌ 駕駛人身分證字號
- ❌ 駕駛人聯絡方式
- ❌ 繳款資訊
- ❌ 收件人資訊

### 二、去識別化處理

#### 地址資訊
**原始資料**：
```
台南市新化區中正路123號前
```

**去識別化後**：
```
行政區：新化區
地點描述：中正路（近中山路路口）
```

**處理規則**：
- ✅ 保留行政區（統計用）
- ✅ 保留路名/路口（聚合點位用）
- ❌ 移除門牌號
- ❌ 移除巷弄號

#### 年齡資訊（僅儲存年齡組）
**原始資料**：
```
年齡：67歲
```

**去識別化後**：
```
年齡組：65+
是否高齡者：是
```

**年齡組分類**：
- `<18`：未成年
- `18-24`：青年
- `25-44`：壯年
- `45-64`：中年
- `65+`：高齡者（重點防治）
- `未知`：資料缺失

**重要**：不儲存精確年齡，僅儲存年齡組

### 三、保留統計必要欄位

#### 允許儲存的統計資訊

| 類別 | 欄位 | 說明 | 個資風險 |
|------|------|------|---------|
| **時間** | 發生日期時間 | 統計必要 | ✅ 無 |
| **時間** | 班別（01-12） | 統計必要 | ✅ 無 |
| **時間** | 年/月/星期 | 統計必要 | ✅ 無 |
| **地點** | 行政區 | 統計必要 | ✅ 無 |
| **地點** | 路口/路段描述 | 點位聚合必要 | ✅ 無（已去識別化） |
| **地點** | 座標（經緯度） | 地圖展示必要 | ✅ 無（無門牌號） |
| **事故** | 嚴重度（A1/A2/A3） | 統計必要 | ✅ 無 |
| **違規** | 違規條款代碼/名稱 | 主題識別必要 | ✅ 無 |
| **駕駛** | 年齡組 | 高齡者防治 | ✅ 無（已分組） |
| **駕駛** | 性別 | 統計分析 | ✅ 無 |
| **單位** | 舉發單位 | 統計必要 | ✅ 無 |

---

## 📊 資料庫設計

### 核心資料表

#### 1. `core_crash`（交通事故）
```sql
-- 僅保留統計必要欄位
CREATE TABLE core_crash (
    id SERIAL PRIMARY KEY,                  -- 系統ID（非原始案件編號）

    -- 時間資訊
    occurred_date DATE NOT NULL,
    occurred_time TIMESTAMP NOT NULL,
    shift_id VARCHAR(2) NOT NULL,          -- 班別 01-12

    -- 地點資訊（去識別化）
    district VARCHAR(50),                   -- 行政區
    location_desc VARCHAR(200),             -- 路口/路段（無門牌號）
    geom GEOMETRY(Point, 4326),            -- 座標
    site_id INTEGER,                        -- 聚合點位ID

    -- 事故資訊
    severity VARCHAR(2) NOT NULL,           -- A1/A2/A3
    severity_weight INTEGER NOT NULL,       -- 5/3/1

    -- 駕駛人特徵（統計用，去識別化）
    driver_age_group VARCHAR(20),           -- <18/18-24/25-44/45-64/65+/未知
    is_elderly BOOLEAN DEFAULT FALSE,       -- 是否高齡者
    driver_gender VARCHAR(10),              -- 男/女/未知

    -- 統計用
    year INTEGER NOT NULL,
    month INTEGER NOT NULL,
    day_of_week INTEGER,
    is_holiday BOOLEAN DEFAULT FALSE
);

-- 無個資：無姓名、身分證、車號、聯絡方式
```

#### 2. `core_ticket`（舉發案件）
```sql
CREATE TABLE core_ticket (
    id SERIAL PRIMARY KEY,                  -- 系統ID（非原始舉發單號）

    -- 時間資訊
    violation_date DATE NOT NULL,
    violation_time TIMESTAMP NOT NULL,
    shift_id VARCHAR(2) NOT NULL,

    -- 地點資訊（去識別化）
    district VARCHAR(50),
    location_desc VARCHAR(200),
    geom GEOMETRY(Point, 4326),
    site_id INTEGER,

    -- 違規資訊
    violation_code VARCHAR(50) NOT NULL,
    violation_name VARCHAR(200),

    -- 主題標籤（自動識別）
    topic_dui BOOLEAN DEFAULT FALSE,        -- 酒駕
    topic_red_light BOOLEAN DEFAULT FALSE,  -- 闖紅燈
    topic_dangerous BOOLEAN DEFAULT FALSE,  -- 危險駕駛

    -- 駕駛人特徵（統計用，去識別化）
    driver_age_group VARCHAR(20),
    is_elderly BOOLEAN DEFAULT FALSE,
    driver_gender VARCHAR(10),

    -- 統計用
    year INTEGER NOT NULL,
    month INTEGER NOT NULL,
    day_of_week INTEGER,
    is_holiday BOOLEAN DEFAULT FALSE,

    -- 舉發單位
    unit_code VARCHAR(50)                   -- 統計用
);

-- 無個資：無舉發單號、車號、駕駛人姓名、身分證、繳款資訊
```

---

## 🔐 資料匯入流程

### 匯入腳本功能

```python
# backend/scripts/import_data.py

def import_crashes(excel_path, db):
    """
    交通事故匯入（個資保護）

    1. 讀取原始 Excel
    2. 僅提取統計必要欄位
    3. 地址去識別化（移除門牌號）
    4. 年齡分組處理（不儲存精確年齡）
    5. 計算班別（12班制）
    6. 寫入資料庫（無個資）
    """
    for row in df.iterrows():
        # 地址去識別化
        district, location_desc = anonymize_location(row['發生地點'])

        # 年齡分組
        age_group, is_elderly = categorize_age(row['年齡'])

        # 性別
        gender = normalize_gender(row['性別'])

        # 創建記錄（無個資）
        crash = Crash(
            occurred_date=row['發生時間'].date(),
            occurred_time=row['發生時間'],
            shift_id=calculate_shift(row['發生時間']),
            district=district,                    # 僅行政區
            location_desc=location_desc,          # 路口/路段（無門牌號）
            severity=row['類別名稱'],
            driver_age_group=age_group,           # 年齡組（非精確年齡）
            is_elderly=is_elderly,
            driver_gender=gender,
            # 無個資：無姓名、車號、身分證等
        )
```

### 個資屏蔽函數

```python
def anonymize_location(address):
    """
    地址去識別化

    輸入：台南市新化區中正路123號前
    輸出：
      district: 新化區
      location_desc: 中正路（近中山路路口）
    """
    # 移除門牌號（數字+號）
    clean_address = re.sub(r'\d+號', '', address)
    clean_address = re.sub(r'第?\d+[巷弄]', '', clean_address)

    return district, location_desc


def categorize_age(age):
    """
    年齡分組（個資保護）

    僅儲存年齡組，不儲存精確年齡
    """
    if age < 18:
        return ('<18', False)
    elif age < 25:
        return ('18-24', False)
    elif age < 45:
        return ('25-44', False)
    elif age < 65:
        return ('45-64', False)
    else:
        return ('65+', True)  # 高齡者
```

---

## 📈 統計分析功能（不需個資）

### 1. 年度同期比較
```sql
-- 範例：2024年1月 vs 2025年1月
SELECT
    year,
    month,
    COUNT(*) AS crash_count,
    SUM(CASE WHEN is_elderly THEN 1 ELSE 0 END) AS elderly_count
FROM core_crash
WHERE month = 1 AND year IN (2024, 2025)
GROUP BY year, month;
```

### 2. 高齡者事故分析
```sql
-- 高齡者事故時段分析
SELECT
    shift_id,
    COUNT(*) AS elderly_crash_count,
    AVG(severity_weight) AS avg_severity
FROM core_crash
WHERE is_elderly = TRUE
GROUP BY shift_id
ORDER BY elderly_crash_count DESC;
```

### 3. 主題違規分析
```sql
-- 酒駕違規班別分布
SELECT
    shift_id,
    COUNT(*) AS dui_count,
    AVG(CASE WHEN is_elderly THEN 1 ELSE 0 END) * 100 AS elderly_percentage
FROM core_ticket
WHERE topic_dui = TRUE
GROUP BY shift_id
ORDER BY dui_count DESC;
```

### 4. Top 5 推薦點位
```sql
-- 推薦執法點位（基於統計資料，無個資）
SELECT
    s.site_name,
    s.district,
    COUNT(DISTINCT t.id) AS ticket_count,
    COUNT(DISTINCT c.id) AS crash_count,
    SUM(CASE WHEN t.topic_dui THEN 1 ELSE 0 END) AS dui_count
FROM dim_site s
LEFT JOIN core_ticket t ON s.id = t.site_id
LEFT JOIN core_crash c ON s.id = c.site_id
WHERE t.shift_id = '11'  -- 班別11
GROUP BY s.id, s.site_name, s.district
ORDER BY (ticket_count + crash_count * 3) DESC
LIMIT 5;
```

---

## ✅ 個資保護驗收清單

### 資料匯入階段
- [ ] 確認原始 Excel 檔案僅供匯入使用，不對外公開
- [ ] 確認匯入後的資料庫不含任何姓名、身分證、車號
- [ ] 確認地址已去識別化（無門牌號）
- [ ] 確認年齡已分組（無精確年齡）

### 資料庫檢查
- [ ] 查詢 `core_crash` 表，確認無個資欄位
- [ ] 查詢 `core_ticket` 表，確認無個資欄位
- [ ] 檢查所有 SELECT 查詢，確認不返回個資

### API 端點檢查
- [ ] 所有 API 回應不含個資
- [ ] Top 5 推薦僅返回統計數據
- [ ] 地圖標記僅顯示點位（無個案詳情）

### 前端顯示檢查
- [ ] 儀表板僅顯示統計圖表
- [ ] 不提供個案查詢功能
- [ ] 不顯示任何可識別個人的資訊

---

## 🔍 與現有系統整合

```
┌─────────────────────────────────────────┐
│  現有個案查詢系統（內建系統）             │
│  • 查詢舉發單號                          │
│  • 查詢車號                              │
│  • 查詢駕駛人                            │
│  • 完整個資（受權限控管）                │
└─────────────────────────────────────────┘
              ↕
        資料來源分離
              ↕
┌─────────────────────────────────────────┐
│  精準執法儀表板系統（本系統）             │
│  • 統計分析                              │
│  • 趨勢預測                              │
│  • Top 5 推薦                            │
│  • 無個資（僅統計資料）                  │
└─────────────────────────────────────────┘
```

**職責分離**：
- **現有系統**：個案查詢、詳細資料、完整個資
- **本系統**：統計分析、執法建議、無個資

---

## 📝 法規遵循

### 個人資料保護法遵循

#### 資料蒐集
- ✅ 目的：統計分析、政策制定
- ✅ 最小蒐集原則：僅保留統計必要欄位
- ✅ 去識別化：移除可識別個人的資訊

#### 資料利用
- ✅ 特定目的：精準執法、事故防治
- ✅ 統計用途：年齡組、性別為統計分析用
- ✅ 無個案查詢：不提供個人資料查詢功能

#### 資料安全
- ✅ 存取控制：系統權限管理
- ✅ 資料加密：資料庫連線加密
- ✅ 稽核記錄：操作日誌記錄

---

## 💡 總結

### ✅ 本系統優勢
1. **完全去識別化**：無法識別特定個人
2. **最小資料原則**：僅保留統計必要欄位
3. **職責分離**：與個案查詢系統分離
4. **法規遵循**：符合個資法規定

### 🎯 適用場景
- ✅ 統計分析（去年同期比較）
- ✅ 趨勢預測（違規造勢率）
- ✅ 精準執法建議（Top 5 推薦）
- ✅ 高齡者事故防治
- ✅ 性別統計分析

### ❌ 不適用場景
- ❌ 個案查詢（請使用現有系統）
- ❌ 駕駛人資料查詢
- ❌ 車號查詢
- ❌ 案件追蹤

---

**系統版本**：v1.0
**更新日期**：2026-01-13
**個資保護等級**：高（完全去識別化）

🔒 **本系統設計確保無個資外洩風險**
